# Database (SQLite)
DATABASE_URL="file:./prisma/dev.db"

# JWT Secret — WAJIB diganti dengan nilai acak yang kuat sebelum production
# Generate: openssl rand -base64 32
JWT_SECRET="dev-secret-change-in-production"
